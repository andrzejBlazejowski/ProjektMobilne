<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Shell.PresentationMode="ModalAnimated"
             Title="New Item"
             x:Class="BooksLoan.Views.LoanV.LoanNewPage">
    <ContentPage.Content>
        <StackLayout>

            <Label Text="LoanDate" FontSize="Medium" />
            <Entry Text="{Binding LoanDate, Mode=TwoWay}" FontSize="Medium" />

            <Label Text="ReturnDate" FontSize="Medium" />
            <Entry Text="{Binding ReturnDate, Mode=TwoWay}" FontSize="Medium" />

            <Label Text="FreeDate" FontSize="Medium" />
            <Entry Text="{Binding FreeDate, Mode=TwoWay}" FontSize="Medium" />

            <StackLayout Orientation="Horizontal">
                <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
                <Button Text="Save" Command="{Binding SaveCommand}" HorizontalOptions="FillAndExpand"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>